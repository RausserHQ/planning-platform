summary: Merge-gated idempotent OpenProject publication
description: Dry-runs then applies the exact merge-bound graph through the durable journal.
value:
  modules:
    - id: publish
      value:
        type: script
        path: f/planning/publish_openproject_graph
        input_transforms:
          event: {type: javascript, expr: flow_input.event}
      retry:
        exponential: {attempts: 4, seconds: 1, multiplier: 5, random_factor: 0}
  failure_module:
    id: failure
    value:
      type: script
      path: f/planning/dead_letter
      input_transforms:
        event: {type: javascript, expr: flow_input.event}
        error: {type: javascript, expr: previous_result}
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    event: {type: object, additionalProperties: true}
  required: [event]
